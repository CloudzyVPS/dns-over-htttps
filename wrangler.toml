name = "doh-worker"
main = "src/worker.ts"
compatibility_date = "2024-09-10"

[vars]
# Default upstream; override per environment if desired
UPSTREAM = "https://cloudflare-dns.com/dns-query"

# Routes omitted for workers.dev testing or choose to add routes below
# [routes]
# routes = [{ pattern = "doh.cloudzy.com/*", zone_name = "cloudzy.com" }]
