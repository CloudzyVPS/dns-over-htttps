{
  "name": "doh-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal DoH (DNS-over-HTTPS) Cloudflare Worker",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler publish",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.2025.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0"
  },
  "license": "MIT"
}
